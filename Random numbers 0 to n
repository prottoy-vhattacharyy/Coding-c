#include<stdio.h>
/* rand()%n line will generate 
random values from 0 to n */
int main()
{
    int n;
    scanf("%d",&n);
    for(int i=1;i<=n;i++){
    	printf("%d\n",rand()%n);
    }    
    return 0;
}
